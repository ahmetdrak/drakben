# ╔══════════════════════════════════════════════════════════════════════════╗
# ║                    DRAKBEN - Autonomous Pentesting AI                    ║
# ║                         Requirements for Kali Linux                       ║
# ╚══════════════════════════════════════════════════════════════════════════╝
#
# KURULUM (Kali Linux):
# ----------------------
# 1. python3 -m venv .venv
# 2. source .venv/bin/activate
# 3. pip install -r requirements.txt
#
# OPSİYONEL (vektör bellek + görselleştirme):
#   pip install -r requirements-extra.txt
#
# NOT: curl_cffi derleme gerektirebilir.
# Kali'de: sudo apt install build-essential libffi-dev

# ========== CORE ==========
rich>=14.0.0              # Terminal UI (beautiful output)
requests>=2.31.0          # HTTP client
python-dotenv>=1.0.0      # Environment variables
psutil>=5.9.0             # System monitoring
aiohttp>=3.9.0            # Async HTTP client
aiofiles>=23.0.0          # Async file I/O
typing_extensions>=4.9.0  # Python typing compatibility

# ========== NETWORK & RECON ==========
beautifulsoup4>=4.12.0    # HTML parsing
dnspython>=2.6.0          # DNS queries
python-whois>=0.9.0       # WHOIS lookups

# ========== SECURITY & CRYPTO ==========
pycryptodome>=3.20.0      # Cryptography (AES-GCM, RSA)
keyring>=25.0.0           # Secure credential storage
curl_cffi>=0.7.0          # TLS fingerprint impersonation (bypass WAF)

# ========== LLM INFRASTRUCTURE ==========
tiktoken>=0.7.0           # Token counting (OpenAI tokenizer)
pydantic>=2.6.0           # Output validation & structured models

# ========== UTILITIES ==========
packaging>=23.0           # Version parsing

# ========== ENHANCED CLI ==========
prompt_toolkit>=3.0.40    # Advanced interactive prompts
pyreadline3>=3.5.0; sys_platform == 'win32'  # Windows readline

# ========== TESTING (optional for users) ==========
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0

# ========== AUTO-INSTALLED DEPENDENCIES ==========
# (pip handles these automatically, listed for reference)
# - certifi, charset-normalizer, idna, urllib3 (requests)
# - soupsieve (beautifulsoup4)
# - markdown-it-py, pygments (rich)
# - cffi, pycparser (curl_cffi)
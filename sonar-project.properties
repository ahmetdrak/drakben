sonar.projectKey=ahmetdrak_drakben
sonar.organization=ahmetdrak
sonar.projectName=drakben

# Source paths
sonar.sources=core,modules,llm,drakben.py
sonar.tests=tests

# Python settings
sonar.python.version=3.12

# Exclusions - Exclude virtual environments, cache, and type stubs
sonar.exclusions=**/__pycache__/**,**/venv/**,**/.venv/**,**/logs/**,**/sessions/**,**/*.pyi,**/node_modules/**,**/.mypy_cache/**,**/.pytest_cache/**,**/.ruff_cache/**,**/drakben_vectors/**

# Coverage
sonar.python.coverage.reportPaths=coverage.xml

# Disable SSL-related security rules for pentesting modules (intentionally insecure for offensive use)
# S4830: Server certificates should be verified during SSL/TLS connections
# S5527: Server hostname verification should be enabled
sonar.issue.ignore.multicriteria=e1
sonar.issue.ignore.multicriteria.e1.ruleKey=python:S4830
sonar.issue.ignore.multicriteria.e1.resourceKey=modules/post_exploit.py
